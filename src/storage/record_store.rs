// use crate::record::Record;

// pub trait RecordStore {
//     fn look_up(&self, name: &str) -> Result<Option<Record>, Box<dyn std::error::Error>>;
//     //fn look_up(&self, name: &str) -> anyhow::Result<Option<Record>>;
//     fn insert(&self, record: &Record) -> Result<(), Box<dyn std::error::Error>>;
// }
